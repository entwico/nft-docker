# sample e2e installs run non-interactively; allow dependency build scripts (sharp's
# native binary, esbuild) so `pnpm install` doesn't abort on the build-approval gate.
dangerouslyAllowAllBuilds: true
